---
phase: 03-harvested-catalog-coverage-inventory
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - classes/local/tool_provider.php
  - tests/tool_provider_test.php
autonomous: true
requirements:
  - DISC-03
  - DISC-04
user_setup: []
must_haves:
  truths:
    - "Harvested tools expose normalized MCP metadata including schema, provenance, and mutability hints."
    - "Tool discovery can page and group large catalogs."
    - "Tool projection remains service-scoped even though the snapshot is site-wide."
  artifacts:
    - path: "classes/local/tool_provider.php"
      provides: "catalog projection, pagination, grouping, normalized MCP metadata"
      min_lines: 220
    - path: "tests/tool_provider_test.php"
      provides: "projection and pagination tests"
      min_lines: 260
  key_links:
    - from: "classes/local/tool_provider.php"
      to: "classes/local/catalog/catalog_builder.php"
      via: "tool projection consumes cached harvested catalog"
    - from: "classes/local/tool_provider.php"
      to: "tmp/moodle/lib/external/classes/external_api.php"
      via: "normalized metadata stays aligned with upstream external function info"
---

<objective>
Refactor tool discovery to project from the harvested catalog with grouping, pagination, and normalized metadata.
</objective>

<context>
@.planning/phases/03-harvested-catalog-coverage-inventory/03-CONTEXT.md
@.planning/phases/03-harvested-catalog-coverage-inventory/03-RESEARCH.md
@.planning/phases/03-harvested-catalog-coverage-inventory/03-PATTERNS.md
@.planning/phases/03-harvested-catalog-coverage-inventory/03-01-SUMMARY.md

@classes/local/catalog/catalog_builder.php
@classes/local/tool_provider.php
@tests/tool_provider_test.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor tool provider to use harvested snapshot projection</name>
  <files>classes/local/tool_provider.php</files>
  <read_first>classes/local/catalog/catalog_builder.php, tmp/moodle/lib/external/classes/external_api.php, tests/tool_provider_test.php</read_first>
  <action>Refactor `tool_provider.php` to consume the harvested catalog snapshot instead of querying only raw service-function links. Add service-scoped filtering, cursor pagination, structured grouping, MCP annotations for mutability hints, and vendor-specific provenance metadata for each tool.</action>
  <verify>rtk php -l classes/local/tool_provider.php && rtk rg "nextCursor|annotations|x-moodle|group|limit|cursor" classes/local/tool_provider.php</verify>
  <acceptance_criteria>
    - Tool projection is snapshot-backed.
    - Each tool includes normalized provenance and mutability metadata.
    - Pagination and grouping are implemented.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extend tool provider tests for normalized catalog projection</name>
  <files>tests/tool_provider_test.php</files>
  <read_first>classes/local/tool_provider.php, classes/local/catalog/catalog_builder.php</read_first>
  <action>Extend the existing tool provider tests to cover normalized metadata, service-scoped projection, grouping, and paginated list behavior.</action>
  <verify>rtk php -l tests/tool_provider_test.php && rtk rg "test_.*pagination|test_.*group|test_.*annotations|test_.*provenance" tests/tool_provider_test.php</verify>
  <acceptance_criteria>
    - Tests cover pagination and grouping.
    - Tests cover normalized metadata output.
    - Tests preserve service-scoped filtering.
  </acceptance_criteria>
</task>

</tasks>

<verification>
- [ ] `rtk php -l classes/local/tool_provider.php tests/tool_provider_test.php`
- [ ] `rtk rg "nextCursor|annotations|x-moodle|group|cursor|limit" classes/local/tool_provider.php`
- [ ] `rtk rg "test_.*pagination|test_.*group|test_.*annotations|test_.*provenance" tests/tool_provider_test.php`
</verification>

<output>
After completion, create `.planning/phases/03-harvested-catalog-coverage-inventory/03-02-SUMMARY.md`
</output>
