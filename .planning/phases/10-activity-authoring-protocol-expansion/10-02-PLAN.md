---
phase: 10-activity-authoring-protocol-expansion
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/local/wrapper/activity_service.php
  - tests/activity_service_test.php
autonomous: true
requirements:
  - wrapper_course_add_module
  - wrapper_[module]_read_data
must_haves:
  truths:
    - "Claude can author fully functional activities that appear in the course and gradebook"
    - "Claude can read internal structured data from modules"
  artifacts:
    - path: "classes/local/wrapper/activity_service.php"
      provides: "Activity module lifecycle and reporting wrapper"
      exports: ["add_module", "read_module_data"]
  key_links:
    - from: "classes/local/wrapper/activity_service.php"
      to: "course/modlib.php"
      via: "create_module()"
      pattern: "create_module"
---

<objective>
Implement the activity authoring and reporting parity wrappers.

Purpose: Circumvent the limitations of server-rendered HTML by instantiating Moodle's internal `locallib.php` classes directly.
Output: Service class that manages Moodle modules properly with course cache and gradebook syncing.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-RESEARCH.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Activity Authoring & Reporting Service</name>
  <files>classes/local/wrapper/activity_service.php, tests/activity_service_test.php</files>
  <behavior>
    - Test 1: `wrapper_course_add_module` successfully provisions a new module via `create_module()`.
    - Test 2: `wrapper_[module]_read_data` returns structured submission/grade data from internal classes.
  </behavior>
  <action>Implement `activity_service.php`. Write `add_module()` requiring `course/modlib.php` and calling `create_module($moduleinfo)`. Never use custom DB inserts for module creation. Write `read_module_data()` dynamically instantiating internal module classes (e.g. `new \assign()`) and invoking getter methods like `get_submissions()`. Ensure capability checks `\require_capability()` are performed prior to granting reporting access, and explicitly use the shared pattern `external_api::validate_context($context)` to ensure proper security context initialization.</action>
  <verify>
    <automated>vendor/bin/phpunit --filter activity_service_test</automated>
  </verify>
  <done>Activity service successfully writes and reads using core modlib routines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API->Activity | Course capability restrictions apply when inspecting or creating modules |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-02 | Elevation of Privilege | activity_service.php | mitigate | Enforce `require_capability('moodle/course:manageactivities')` prior to calling add_moduleinfo |
</threat_model>

<verification>
Test suite executes without errors and Moodle caches recognize the newly created modules.
</verification>

<success_criteria>
`create_module()` replaces direct DB inserts to author modules safely with gradebook and event synchronization.
</success_criteria>

<output>
After completion, create `.planning/phases/10-activity-authoring-protocol-expansion/10-02-SUMMARY.md`
</output>