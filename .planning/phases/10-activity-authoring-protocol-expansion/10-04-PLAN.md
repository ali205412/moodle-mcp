---
phase: 10-activity-authoring-protocol-expansion
plan: 04
type: execute
wave: 2
depends_on: ["01", "02", "03"]
files_modified:
  - classes/local/transport/server.php
  - classes/local/wrapper/manager.php
  - tests/transport_server_test.php
autonomous: true
requirements:
  - resources/list
  - resources/read
  - prompts/list
  - prompts/get
must_haves:
  truths:
    - "Manager maps new tool wrappers for execution"
    - "Transport server supports MCP native resources and prompts routing"
  artifacts:
    - path: "classes/local/transport/server.php"
      provides: "Routing logic for resources and prompts"
      contains: "case 'resources/list':"
    - path: "classes/local/wrapper/manager.php"
      provides: "Registry for memory, activity, and discovery tools"
      contains: "new definition"
  key_links:
    - from: "classes/local/transport/server.php"
      to: "classes/local/wrapper/memory_service.php"
      via: "delegates resource reads"
      pattern: "resources/list"
---

<objective>
Wire the new services into the tool registry and extend the MCP protocol in the transport server.

Purpose: Enable MCP clients (Claude) to discover the newly built tools, and implement the native `resources` and `prompts` namespaces.
Output: Integrated `manager.php` with all new definitions and `server.php` handling extended MCP JSON-RPC routes.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-RESEARCH.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-PATTERNS.md
</context>

<interfaces>
From classes/local/wrapper/memory_service.php:
```php
class memory_service {
    public function read_memories(int $userid): array;
    public function write_memory(int $userid, string $content): array;
}
```
From classes/local/wrapper/activity_service.php:
```php
class activity_service {
    public function add_module(array $payload, int $courseid): array;
    public function read_module_data(string $modulename, int $courseid, int $cmid): array;
}
```
From classes/local/wrapper/discovery_service.php:
```php
class discovery_service {
    public function search_api(string $query): array;
    public function execute_api(string $functionname, array $params): array;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Register tools in Manager</name>
  <files>classes/local/wrapper/manager.php</files>
  <action>Update `manager.php` to inject `memory_service`, `activity_service`, and `discovery_service` into the constructor (analog: `badge_service`). Define the new wrapper definitions in `registry()`: `wrapper_memory_write`, `wrapper_course_add_module`, `wrapper_[module]_read_data`, `wrapper_moodle_api_search`, and `wrapper_moodle_api_execute`. Ensure proper parameter schemas are attached. Route their execution correctly in the `execute()` match statement.</action>
  <verify>
    <automated>php admin/cli/cfg.php --component=webservice_mcp --show</automated>
  </verify>
  <done>All newly built services are registered as tools.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Implement Protocol Expansion</name>
  <files>classes/local/transport/server.php, tests/transport_server_test.php</files>
  <action>Update `handle_transport_method()` in `server.php` to support `resources/list`, `resources/read`, `prompts/list`, and `prompts/get`. For resources, interact with `memory_service` to fetch the user's memory content. For prompts, define a static list of specialized instructions as requested. Update `transport_server_test.php` to verify routing and return structures.</action>
  <verify>
    <automated>vendor/bin/phpunit --filter transport_server_test</automated>
  </verify>
  <done>Server successfully handles resource and prompt JSON-RPC requests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Network->Transport | New JSON-RPC routes must authenticate the user session correctly |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-05 | Elevation of Privilege | server.php | mitigate | Ensure `$this->ensure_oauth_scope()` is called for `resources/` and `prompts/` routes |
</threat_model>

<verification>
`tools/list` returns the new definitions, and `resources/list` returns the valid memory scope.
</verification>

<success_criteria>
The 4 phase capabilities (memory, discovery, activity authoring, extended MCP protocol) are fully integrated and accessible to the LLM context.
</success_criteria>

<output>
After completion, create `.planning/phases/10-activity-authoring-protocol-expansion/10-04-SUMMARY.md`
</output>