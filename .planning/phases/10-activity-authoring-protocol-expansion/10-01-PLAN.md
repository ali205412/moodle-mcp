---
phase: 10-activity-authoring-protocol-expansion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/install.xml
  - db/upgrade.php
  - version.php
  - classes/local/wrapper/memory_service.php
  - tests/memory_service_test.php
autonomous: true
requirements:
  - webservice_mcp_memory
must_haves:
  truths:
    - "Memory table schema is defined and deployed"
    - "Memory CRUD service validates user ownership of memories"
  artifacts:
    - path: "classes/local/wrapper/memory_service.php"
      provides: "CRUD operations on memory table"
      exports: ["write_memory", "read_memories", "read_memory_by_id", "update_memory", "delete_memory"]
  key_links:
    - from: "classes/local/wrapper/memory_service.php"
      to: "webservice_mcp_memory"
      via: "database query"
      pattern: "\$DB->insert_record"
---

<objective>
Establish persistent knowledgebase storage via a new memory table and service layer.

Purpose: Claude needs a mechanism to save user-scoped context securely.
Output: DB migrations, memory service wrapper, and test suite.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-RESEARCH.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Database Migration for webservice_mcp_memory</name>
  <files>db/install.xml, db/upgrade.php, version.php</files>
  <action>Define the `webservice_mcp_memory` table in `install.xml` following the exact pattern used for `webservice_mcp_credential`. Include fields: `id` (int/primary), `userid` (int/foreign key), `content` (text), `timecreated` (int), `timemodified` (int). Implement the upgrade block in `upgrade.php` and bump the plugin version in `version.php` to trigger the upgrade.</action>
  <verify>
    <automated>php admin/cli/upgrade.php --non-interactive</automated>
  </verify>
  <done>Table schema created and plugin upgraded successfully</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Memory Service Wrapper</name>
  <files>classes/local/wrapper/memory_service.php, tests/memory_service_test.php</files>
  <behavior>
    - Test 1: write_memory persists content scoped to the current user.
    - Test 2: read_memories retrieves only the user's own memories, not others.
    - Test 3: read_memory_by_id, update_memory, and delete_memory strictly validate ownership before executing.
  </behavior>
  <action>Create `memory_service.php` with `write_memory`, `read_memories`, `read_memory_by_id`, `update_memory`, and `delete_memory` methods. These additional methods are critical to fully support the `resources/read` MCP route. Crucially, enforce `$USER->id` checks so users cannot read/write memories belonging to someone else. Add `external_api::validate_context` checks as defined in Phase 10 PATTERNS. Write the PHPUnit test leveraging `advanced_testcase` to reset the database and test DB teardown of the memory table.</action>
  <verify>
    <automated>vendor/bin/phpunit --filter memory_service_test</automated>
  </verify>
  <done>Service validates ownership and persists user context appropriately with full CRUD operations</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Service->DB | Memory queries must strictly filter by the authenticated user session |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Elevation of Privilege | memory_service.php | mitigate | Ensure `userid` check on all `webservice_mcp_memory` SELECTs/UPDATEs/DELETEs to prevent IDOR on memory operations |
</threat_model>

<verification>
Ensure phpunit runs successfully on the newly added test suite and Moodle environment accepts the DB upgrade.
</verification>

<success_criteria>
The database has a memory table, and the memory service allows authenticated user-scoped CRUD operations.
</success_criteria>

<output>
After completion, create `.planning/phases/10-activity-authoring-protocol-expansion/10-01-SUMMARY.md`
</output>