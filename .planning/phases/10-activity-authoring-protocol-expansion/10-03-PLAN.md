---
phase: 10-activity-authoring-protocol-expansion
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - classes/local/wrapper/discovery_service.php
  - tests/discovery_service_test.php
autonomous: true
requirements:
  - wrapper_moodle_api_search
  - wrapper_moodle_api_execute
must_haves:
  truths:
    - "Claude can discover and safely execute any Moodle API function"
    - "Discovery engine casts and validates payloads using external_function_info"
  artifacts:
    - path: "classes/local/wrapper/discovery_service.php"
      provides: "Dynamic API searching and validated execution"
      exports: ["search_api", "execute_api"]
  key_links:
    - from: "classes/local/wrapper/discovery_service.php"
      to: "external_api"
      via: "dynamic validation"
      pattern: "external_api::validate_parameters"
---

<objective>
Solve context exhaustion with a Tool Discovery Engine.

Purpose: Prevent broadcasting 800+ raw Moodle API functions directly into MCP `tools/list`.
Output: Discovery service that safely searches and executes valid external API functions.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-RESEARCH.md
@.planning/phases/10-activity-authoring-protocol-expansion/10-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build Tool Discovery Engine</name>
  <files>classes/local/wrapper/discovery_service.php, tests/discovery_service_test.php</files>
  <behavior>
    - Test 1: `search_api` correctly filters external functions by query term.
    - Test 2: `execute_api` fetches parameter schema, validates JSON, and executes function correctly.
  </behavior>
  <action>Create `discovery_service.php` containing `search_api($query)` returning function names/signatures, and `execute_api($functionname, $params)`. The `execute_api` method MUST use `external_api::external_function_info` to get parameter descriptions, run `external_api::validate_parameters` to safely cast JSON arguments, explicitly use the shared pattern `external_api::validate_context($context)` to ensure proper security context initialization before execution, execute via `call_user_func_array`, and format output using `external_api::clean_returnvalue`.</action>
  <verify>
    <automated>vendor/bin/phpunit --filter discovery_service_test</automated>
  </verify>
  <done>External functions are executed with native validation rules strictly enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Search->Execute | Calling arbitrary Moodle functions requires robust validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Denial of Service | discovery_service.php | mitigate | Only list available functions upon search rather than broadcasting all at once |
| T-10-04 | Elevation of Privilege | discovery_service.php | mitigate | Allow execution only for functions the current user context holds valid Moodle capabilities for |
</threat_model>

<verification>
Validation mechanisms correctly reject malformed payloads without PHP fatal errors.
</verification>

<success_criteria>
Discovery API serves as a secure proxy to Moodle's 800+ core functions.
</success_criteria>

<output>
After completion, create `.planning/phases/10-activity-authoring-protocol-expansion/10-03-SUMMARY.md`
</output>