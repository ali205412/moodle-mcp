# Phase 09 Human UAT

## Pending Site Checks

1. Connect through the real Moodle connector flow and confirm the new wrapper tools appear for a privileged operator account.
2. Create and edit a supported question type through MCP, then open the returned preview URL and verify the authored version matches the request.
3. Create a manual grade item through MCP, move it into a non-root category, and confirm the gradebook setup UI reflects the change.
4. Create an inactive badge through MCP, add related badges/alignment, then manually award and revoke an active badge for a test user.
5. Confirm the remaining unsupported areas listed in `09-PARITY-AUDIT.md` are acceptable and explicit enough for release notes.
