---
phase: 01-identity-bootstrap-connector-credentials
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/install.xml
  - db/upgrade.php
  - version.php
  - classes/local/auth/credential_manager.php
  - tests/credential_manager_test.php
autonomous: true
requirements:
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - "Connector can issue a plugin-managed credential record for an authenticated Moodle user instead of exposing raw permanent-token UX as the product contract."
    - "Issued credentials carry enough service/context/session metadata for later transport-side validation."
    - "Credential issuance and lifecycle rules are covered by automated tests."
  artifacts:
    - path: "db/install.xml"
      provides: "Connector credential persistence schema"
      min_lines: 20
    - path: "db/upgrade.php"
      provides: "Upgrade path for credential persistence schema"
      min_lines: 20
    - path: "classes/local/auth/credential_manager.php"
      provides: "Credential issuance and revocation manager"
      min_lines: 80
    - path: "tests/credential_manager_test.php"
      provides: "Automated coverage for credential lifecycle rules"
      min_lines: 60
  key_links:
    - from: "db/install.xml"
      to: "classes/local/auth/credential_manager.php"
      via: "credential_manager reads and writes the connector credential table and stores service/context/session-linked metadata"
    - from: "classes/local/auth/credential_manager.php"
      to: "tests/credential_manager_test.php"
      via: "tests assert short-lived/session-linked issuance and explicit durable-grant behavior"
---

<objective>
Create the credential persistence and service layer that Phase 1 bootstrap and later transport code will rely on.

Purpose: Establish a plugin-managed credential contract per D-05, D-06, D-07, D-09, and D-10 before any browser bootstrap entrypoint or transport-side identity resolver is wired.
Output: Connector credential schema, upgrade path, credential manager service, and automated tests for issuance semantics.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-RESEARCH.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-PATTERNS.md

@version.php
@db/access.php
@tmp/moodle/lib/external/classes/util.php
@tmp/moodle/webservice/lib.php
@tmp/moodle/lib/db/install.xml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create credential persistence schema and upgrade path</name>
  <files>db/install.xml, db/upgrade.php, version.php</files>
  <read_first>db/access.php, version.php, tmp/moodle/lib/db/install.xml, tmp/moodle/lib/external/classes/util.php, .planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md</read_first>
  <action>Create a plugin-owned persistence model for connector credentials per D-05, D-06, D-09, and D-10. Add a new table to `db/install.xml` for connector-managed credentials with concrete fields for `userid`, `serviceidentifier`, `contextid`, `tokentype`, `sid`, `validuntil`, `iprestriction`, `timecreated`, `timemodified`, `lastaccess`, `revoked`, and a human-readable `name`/label. Add `db/upgrade.php` so the table is created on upgrade for existing installs. Bump `version.php` so the upgrade path runs. Copy the shape of service/context/session metadata from `tmp/moodle/lib/external/classes/util.php` instead of inventing incompatible field semantics, but do not reuse the core `external_tokens` table as the plugin product contract.</action>
  <verify>php -l db/upgrade.php && php -l version.php && rtk rg "TABLE NAME=.*connector|TABLE NAME=.*credential" db/install.xml && rtk rg "function xmldb_webservice_mcp_upgrade" db/upgrade.php</verify>
  <acceptance_criteria>
    - `db/install.xml` defines a plugin-owned credential table with concrete fields for user, service/context binding, session linkage, expiry, IP restriction, and revocation state.
    - `db/upgrade.php` contains `xmldb_webservice_mcp_upgrade` and an upgrade step for the new credential table.
    - `version.php` has a higher `$plugin->version` than the current baseline.
  </acceptance_criteria>
  <done>Plugin schema and upgrade path exist and encode connector-managed credential lifecycle metadata explicitly.</done>
</task>

<task type="auto">
  <name>Task 2: Implement credential manager service around Moodle token primitives</name>
  <files>classes/local/auth/credential_manager.php</files>
  <read_first>classes/local/server.php, tmp/moodle/lib/external/classes/util.php, tmp/moodle/webservice/lib.php, .planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md, .planning/phases/01-identity-bootstrap-connector-credentials/01-RESEARCH.md</read_first>
  <action>Create `classes/local/auth/credential_manager.php` as the Phase 1 authority for issuing, looking up, revoking, and describing connector credentials. Make it explicit in code comments and naming that this is a plugin-managed contract per D-05 and not raw permanent-token UX. Reuse Moodle token concepts from `tmp/moodle/lib/external/classes/util.php` — especially `externalserviceid`, `contextid`, `sid`, `validuntil`, and `iprestriction` semantics — but wrap them in methods such as `issue_bootstrap_credential`, `issue_durable_grant`, `resolve_credential`, and `revoke_credential`. Implement default issuance as session-linked or short-lived per D-06, and require an explicit durable-grant path per D-07 instead of auto-minting a long-lived remote secret on normal login.</action>
  <verify>php -l classes/local/auth/credential_manager.php && rtk rg "issue_bootstrap_credential|issue_durable_grant|resolve_credential|revoke_credential" classes/local/auth/credential_manager.php</verify>
  <acceptance_criteria>
    - `classes/local/auth/credential_manager.php` exists and exposes explicit methods for bootstrap issuance, durable grant issuance, lookup, and revocation.
    - The code stores service/context/session metadata rather than treating the credential as an unscoped bearer string.
    - The code comments or method names make the durable-grant path explicit instead of silently issuing long-lived credentials on every login.
  </acceptance_criteria>
  <done>Credential manager service exists with clear bootstrap vs durable issuance paths and plugin-owned lifecycle semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated credential lifecycle tests</name>
  <files>tests/credential_manager_test.php</files>
  <read_first>tests/server_test.php, tmp/moodle/lib/external/classes/util.php, tmp/moodle/webservice/lib.php, classes/local/auth/credential_manager.php</read_first>
  <action>Create `tests/credential_manager_test.php` using the existing PHPUnit style from `tests/server_test.php`. Cover at minimum: session-linked/short-lived bootstrap issuance, explicit durable grant issuance, revocation behavior, and service/context metadata persistence. The assertions must verify the connector manager preserves Phase 1 decisions D-05 through D-10 rather than collapsing into a raw permanent-token shortcut.</action>
  <verify>php -l tests/credential_manager_test.php && rtk rg "test_.*bootstrap|test_.*durable|test_.*revoke" tests/credential_manager_test.php</verify>
  <acceptance_criteria>
    - `tests/credential_manager_test.php` exists and follows the repo's PHPUnit naming/style conventions.
    - The test file contains separate coverage for bootstrap issuance, durable issuance, and revocation behavior.
    - The tests assert stored service/context/session metadata, not just token-string existence.
  </acceptance_criteria>
  <done>Automated tests define the expected Phase 1 credential lifecycle contract.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php -l db/upgrade.php version.php classes/local/auth/credential_manager.php tests/credential_manager_test.php`
- [ ] `rtk rg "issue_bootstrap_credential|issue_durable_grant|revoke_credential" classes/local/auth/credential_manager.php`
- [ ] `rtk rg "test_.*bootstrap|test_.*durable|test_.*revoke" tests/credential_manager_test.php`
</verification>

<success_criteria>

- All tasks completed
- Credential persistence and lifecycle code exist
- Bootstrap vs durable issuance paths are explicit
- Automated tests define and protect the Phase 1 credential contract
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-bootstrap-connector-credentials/01-01-SUMMARY.md`
</output>
