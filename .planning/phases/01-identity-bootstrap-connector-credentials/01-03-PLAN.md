---
phase: 01-identity-bootstrap-connector-credentials
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - settings.php
  - db/access.php
  - classes/local/auth/transport_identity.php
  - classes/local/auth/credential_admin_service.php
  - classes/local/auth/companion_contract.php
  - tests/auth_admin_test.php
autonomous: true
requirements:
  - AUTH-03
  - AUTH-05
user_setup: []
must_haves:
  truths:
    - "Operators can inspect and revoke connector credentials without deleting the Moodle user."
    - "The transport side has an explicit identity-resolution contract based on connector credentials and restricted service/context semantics."
    - "The Phase 1 companion seam exists as a non-authoritative contract, not as a replacement source of truth."
  artifacts:
    - path: "settings.php"
      provides: "Connector auth/seam settings"
      min_lines: 30
    - path: "db/access.php"
      provides: "Connector management capability additions"
      min_lines: 20
    - path: "classes/local/auth/transport_identity.php"
      provides: "Runtime identity resolution for connector credentials"
      min_lines: 60
    - path: "classes/local/auth/credential_admin_service.php"
      provides: "Inspection and revocation operations"
      min_lines: 60
    - path: "classes/local/auth/companion_contract.php"
      provides: "Phase 1 non-authoritative companion seam interface"
      min_lines: 40
    - path: "tests/auth_admin_test.php"
      provides: "Automated coverage for inspect/revoke and companion boundary invariants"
      min_lines: 60
  key_links:
    - from: "classes/local/auth/credential_admin_service.php"
      to: "classes/local/auth/credential_manager.php"
      via: "admin service reads and revokes plugin-managed credentials rather than manipulating generic token UX directly"
    - from: "classes/local/auth/transport_identity.php"
      to: "tmp/moodle/webservice/lib.php"
      via: "transport identity resolution mirrors restricted service/context token validation semantics"
    - from: "classes/local/auth/companion_contract.php"
      to: "classes/local/auth/transport_identity.php"
      via: "companion seam depends on plugin-side identity resolution and does not replace it"
---

<objective>
Add operator-facing credential management and define the non-authoritative companion seam boundary for Phase 1.

Purpose: Satisfy AUTH-05 and the accepted Phase 1 decision that the companion-service seam must be included now while Moodle remains the authority for auth, discovery, and execution.
Output: Settings/capability additions, transport identity service, credential inspection/revocation service, companion seam contract, and tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-RESEARCH.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-PATTERNS.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-01-SUMMARY.md

@db/access.php
@tmp/moodle/webservice/lib.php
@tmp/moodle/lib/external/classes/util.php
@tmp/moodle/admin/webservice/tokens.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add connector management settings and inspect/revoke service</name>
  <files>settings.php, db/access.php, classes/local/auth/credential_admin_service.php</files>
  <read_first>db/access.php, tmp/moodle/admin/webservice/tokens.php, classes/local/auth/credential_manager.php, .planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md</read_first>
  <action>Create `settings.php` for Phase 1 auth-related toggles such as enabling durable remote grants and enabling the companion seam. Extend `db/access.php` with a connector-management capability that can guard inspection and revocation actions separately from plain `webservice/mcp:use`. Implement `classes/local/auth/credential_admin_service.php` to list, inspect, and revoke plugin-managed connector credentials per D-08 and D-11, using the credential manager from plan `01-01` rather than raw generic token admin UX from Moodle core.</action>
  <verify>php -l settings.php && php -l db/access.php && php -l classes/local/auth/credential_admin_service.php && rtk rg "manage|revoke|inspect" db/access.php classes/local/auth/credential_admin_service.php</verify>
  <acceptance_criteria>
    - `settings.php` exists and contains connector auth/seam settings rather than hardcoded behavior only.
    - `db/access.php` contains a management capability distinct from `webservice/mcp:use`.
    - `classes/local/auth/credential_admin_service.php` exposes explicit inspect/revoke operations for connector-managed credentials.
  </acceptance_criteria>
  <done>Operator-facing credential inspection and revocation boundary exists and is not collapsed into generic Moodle token admin UX.</done>
</task>

<task type="auto">
  <name>Task 2: Implement transport identity resolver and companion seam contract</name>
  <files>classes/local/auth/transport_identity.php, classes/local/auth/companion_contract.php</files>
  <read_first>tmp/moodle/webservice/lib.php, tmp/moodle/lib/external/classes/util.php, classes/local/server.php, .planning/phases/01-identity-bootstrap-connector-credentials/01-RESEARCH.md</read_first>
  <action>Create `classes/local/auth/transport_identity.php` to resolve connector credentials into runtime user, restricted service, and restricted context data using the same concrete semantics Phase 1 adopted from `tmp/moodle/webservice/lib.php` and `tmp/moodle/lib/external/classes/util.php`. Create `classes/local/auth/companion_contract.php` as the explicit interface/boundary for the companion seam per D-11: it may describe transport-facing bootstrap exchange and session metadata, but it must not own final authorization, discovery, or execution. Make those boundaries explicit in interface names, method docblocks, and comments.</action>
  <verify>php -l classes/local/auth/transport_identity.php && php -l classes/local/auth/companion_contract.php && rtk rg "restricted_service|restricted_context|companion" classes/local/auth/transport_identity.php classes/local/auth/companion_contract.php</verify>
  <acceptance_criteria>
    - `classes/local/auth/transport_identity.php` exists and models restricted service/context identity resolution.
    - `classes/local/auth/companion_contract.php` exists and documents a non-authoritative seam.
    - Neither file assigns discovery or permission authority to the companion seam.
  </acceptance_criteria>
  <done>Phase 1 includes an explicit transport identity adapter and a bounded companion seam contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for admin and seam invariants</name>
  <files>tests/auth_admin_test.php</files>
  <read_first>tests/server_test.php, classes/local/auth/credential_admin_service.php, classes/local/auth/transport_identity.php, classes/local/auth/companion_contract.php</read_first>
  <action>Create `tests/auth_admin_test.php` with PHPUnit coverage for inspection/revocation behavior and for the key architectural invariant that the companion seam does not become the authority for permission checks. Cover at minimum: revoking a connector credential, listing connector credentials for inspection, and verifying that transport identity resolution depends on plugin-side credential/service/context data instead of bypassing into a raw edge-only model.</action>
  <verify>php -l tests/auth_admin_test.php && rtk rg "test_.*revoke|test_.*inspect|test_.*companion" tests/auth_admin_test.php</verify>
  <acceptance_criteria>
    - `tests/auth_admin_test.php` exists and follows repo PHPUnit conventions.
    - The test file contains explicit coverage for revoke/inspect behavior.
    - The test file contains at least one assertion around the non-authoritative companion seam boundary.
  </acceptance_criteria>
  <done>Phase 1 management and companion-boundary behavior is defined by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php -l settings.php db/access.php classes/local/auth/credential_admin_service.php classes/local/auth/transport_identity.php classes/local/auth/companion_contract.php tests/auth_admin_test.php`
- [ ] `rtk rg "manage|revoke|inspect" db/access.php classes/local/auth/credential_admin_service.php`
- [ ] `rtk rg "restricted_service|restricted_context|companion" classes/local/auth/transport_identity.php classes/local/auth/companion_contract.php`
- [ ] `rtk rg "test_.*revoke|test_.*inspect|test_.*companion" tests/auth_admin_test.php`
</verification>

<success_criteria>

- All tasks completed
- Operators can inspect and revoke connector-managed credentials
- Transport identity resolution is explicit and source-backed
- Companion seam is present but non-authoritative by design
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-bootstrap-connector-credentials/01-03-SUMMARY.md`
</output>
