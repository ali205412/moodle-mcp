---
phase: 01-identity-bootstrap-connector-credentials
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - launch.php
  - classes/local/auth/bootstrap_service.php
  - classes/local/auth/oauth_bridge.php
  - lang/en/webservice_mcp.php
  - tests/launch_test.php
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - "User can start connector access through a Moodle page flow instead of creating a permanent token manually."
    - "Existing Moodle session or Moodle-managed SSO can complete connector bootstrap without a second credential prompt."
    - "Bootstrap flow ends by issuing connector access through the plugin credential manager rather than by exposing raw token admin UX."
  artifacts:
    - path: "launch.php"
      provides: "Interactive connector bootstrap page"
      min_lines: 50
    - path: "classes/local/auth/bootstrap_service.php"
      provides: "Bootstrap orchestration service"
      min_lines: 80
    - path: "classes/local/auth/oauth_bridge.php"
      provides: "Moodle OAuth redirect helper for connector bootstrap"
      min_lines: 40
    - path: "tests/launch_test.php"
      provides: "Automated coverage for bootstrap path"
      min_lines: 60
  key_links:
    - from: "launch.php"
      to: "classes/local/auth/bootstrap_service.php"
      via: "launch.php delegates bootstrap decisions and issuance to the bootstrap service"
    - from: "classes/local/auth/bootstrap_service.php"
      to: "classes/local/auth/credential_manager.php"
      via: "bootstrap service exchanges authenticated Moodle session for connector-managed credential"
    - from: "classes/local/auth/oauth_bridge.php"
      to: "tmp/moodle/auth/oauth2/login.php"
      via: "OAuth bridge follows Moodle-native issuer redirect and return flow"
---

<objective>
Implement the interactive bootstrap entrypoint and its Moodle-native SSO handoff behavior.

Purpose: Deliver AUTH-01, AUTH-02, and AUTH-04 by adding a proper browser/session bootstrap path instead of forcing users through manual token creation or a WS-only endpoint.
Output: `launch.php`, bootstrap service/orchestration classes, updated strings, and automated tests for bootstrap behavior.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-RESEARCH.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-PATTERNS.md
@.planning/phases/01-identity-bootstrap-connector-credentials/01-01-SUMMARY.md

@server.php
@classes/local/server.php
@db/access.php
@tmp/moodle/admin/tool/mobile/launch.php
@tmp/moodle/auth/oauth2/login.php
@tmp/moodle/lib/moodlelib.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated launch entrypoint and bootstrap service</name>
  <files>launch.php, classes/local/auth/bootstrap_service.php, lang/en/webservice_mcp.php</files>
  <read_first>server.php, classes/local/server.php, db/access.php, tmp/moodle/admin/tool/mobile/launch.php, tmp/moodle/lib/moodlelib.php, .planning/phases/01-identity-bootstrap-connector-credentials/01-CONTEXT.md</read_first>
  <action>Create `launch.php` as a normal Moodle page controller per D-01, D-02, D-03, and D-04. It must `require('../../config.php')`, use `require_login(0, false)` rather than `WS_SERVER`, and explicitly gate access with `webservice/mcp:use`. Move the page-flow logic into `classes/local/auth/bootstrap_service.php` so the controller remains thin. The bootstrap service should validate the request, require an active user, determine whether the user already has a valid session, and then call the credential manager from plan `01-01` to issue connector access instead of surfacing raw permanent-token creation UX. Add any needed user-facing strings to `lang/en/webservice_mcp.php` for bootstrap success and denial messaging.</action>
  <verify>php -l launch.php && php -l classes/local/auth/bootstrap_service.php && rtk rg "require_login\\(0, false\\)|webservice/mcp:use|bootstrap_service" launch.php classes/local/auth/bootstrap_service.php</verify>
  <acceptance_criteria>
    - `launch.php` exists and does not define `WS_SERVER`.
    - `launch.php` or its delegated service uses `require_login(0, false)` and checks `webservice/mcp:use`.
    - `classes/local/auth/bootstrap_service.php` exists and is the service responsible for issuing connector access after login.
  </acceptance_criteria>
  <done>Interactive bootstrap path exists as a normal Moodle page flow and hands off credential issuance to the plugin auth service.</done>
</task>

<task type="auto">
  <name>Task 2: Add Moodle-native OAuth/SSO handoff support to bootstrap</name>
  <files>classes/local/auth/oauth_bridge.php, launch.php, classes/local/auth/bootstrap_service.php</files>
  <read_first>tmp/moodle/auth/oauth2/login.php, tmp/moodle/admin/tool/mobile/launch.php, launch.php, classes/local/auth/bootstrap_service.php, .planning/phases/01-identity-bootstrap-connector-credentials/01-RESEARCH.md</read_first>
  <action>Create `classes/local/auth/oauth_bridge.php` that follows Moodle's own issuer-driven OAuth redirect pattern per D-12. The bridge should construct a return URL back into `launch.php`, detect when an OAuth issuer-driven bootstrap path is requested, and redirect through Moodle-managed OAuth login instead of inventing a custom connector SSO protocol. Wire `launch.php` and the bootstrap service to use this helper only when needed, while preserving the simpler path for an already-authenticated Moodle session per D-02.</action>
  <verify>php -l classes/local/auth/oauth_bridge.php && rtk rg "auth/oauth2/login.php|wantsurl|issuer" classes/local/auth/oauth_bridge.php launch.php classes/local/auth/bootstrap_service.php</verify>
  <acceptance_criteria>
    - `classes/local/auth/oauth_bridge.php` exists and references Moodle's OAuth login path semantics.
    - `launch.php` can route OAuth issuer-based bootstrap through the bridge and back to the connector bootstrap flow.
    - The already-authenticated session path remains simpler and does not force an unnecessary second credential prompt.
  </acceptance_criteria>
  <done>Bootstrap path supports Moodle-native OAuth/SSO redirect handoff without inventing a separate connector SSO flow.</done>
</task>

<task type="auto">
  <name>Task 3: Add bootstrap-path automated tests</name>
  <files>tests/launch_test.php</files>
  <read_first>tests/server_test.php, launch.php, classes/local/auth/bootstrap_service.php, classes/local/auth/oauth_bridge.php</read_first>
  <action>Create `tests/launch_test.php` using existing PHPUnit conventions. Cover at minimum: denying access when the user lacks `webservice/mcp:use`, successful bootstrap when a user is already logged in, and the redirect/handoff path for OAuth issuer-based login requests. Keep the assertions tied to the actual bootstrap service responsibilities, not just superficial file existence.</action>
  <verify>php -l tests/launch_test.php && rtk rg "test_.*bootstrap|test_.*oauth|test_.*capability" tests/launch_test.php</verify>
  <acceptance_criteria>
    - `tests/launch_test.php` exists and follows the repo's PHPUnit structure.
    - Test names explicitly cover capability denial, already-authenticated bootstrap, and OAuth handoff behavior.
    - The test file references the new bootstrap entrypoint/service instead of only existing MCP server classes.
  </acceptance_criteria>
  <done>Bootstrap entrypoint behavior is pinned by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `php -l launch.php classes/local/auth/bootstrap_service.php classes/local/auth/oauth_bridge.php tests/launch_test.php`
- [ ] `rtk rg "require_login\\(0, false\\)|webservice/mcp:use" launch.php classes/local/auth/bootstrap_service.php`
- [ ] `rtk rg "auth/oauth2/login.php|wantsurl|issuer" classes/local/auth/oauth_bridge.php`
- [ ] `rtk rg "test_.*bootstrap|test_.*oauth|test_.*capability" tests/launch_test.php`
</verification>

<success_criteria>

- All tasks completed
- Launch/bootstrap path exists as a proper Moodle page flow
- Moodle-native OAuth/SSO handoff is wired without custom auth protocol invention
- Bootstrap behavior is covered by automated tests
</success_criteria>

<output>
After completion, create `.planning/phases/01-identity-bootstrap-connector-credentials/01-02-SUMMARY.md`
</output>
