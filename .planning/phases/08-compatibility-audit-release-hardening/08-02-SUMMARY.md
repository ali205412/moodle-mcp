# Summary 08-02

Transport discovery and tool execution now emit stable audit ids backed by a persisted plugin audit table, giving operators a traceable server-side record for discovery and mutations.
