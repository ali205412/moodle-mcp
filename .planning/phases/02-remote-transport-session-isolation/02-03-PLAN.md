---
phase: 02-remote-transport-session-isolation
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified:
  - sse.php
  - classes/local/transport/sse_controller.php
  - tests/sse_transport_test.php
autonomous: true
requirements:
  - TRAN-02
  - TRAN-04
user_setup: []
must_haves:
  truths:
    - "Sites that enable compatibility mode can expose a separate legacy SSE endpoint without changing auth or permission semantics."
    - "SSE compatibility shares plugin-owned session/replay state rather than inventing a separate transport authority."
    - "SSE compatibility behavior is covered by automated tests."
  artifacts:
    - path: "sse.php"
      provides: "Legacy SSE compatibility endpoint"
      min_lines: 30
    - path: "classes/local/transport/sse_controller.php"
      provides: "SSE compatibility controller over plugin transport state"
      min_lines: 80
    - path: "tests/sse_transport_test.php"
      provides: "Automated coverage for SSE compatibility behavior"
      min_lines: 70
  key_links:
    - from: "sse.php"
      to: "classes/local/transport/sse_controller.php"
      via: "entrypoint delegates SSE compatibility behavior to a dedicated controller"
    - from: "classes/local/transport/sse_controller.php"
      to: "classes/local/stream/session_store.php"
      via: "SSE compatibility validates plugin-owned transport session state"
    - from: "classes/local/transport/sse_controller.php"
      to: "classes/local/stream/replay_store.php"
      via: "SSE compatibility can fetch replay or buffered messages without owning the source of truth"
---

<objective>
Add the separate legacy SSE compatibility path over the Phase 2 transport/session foundation.

Purpose: Deliver TRAN-02 while keeping SSE strictly a compatibility adapter instead of the main transport design.
Output: Dedicated SSE endpoint and controller plus tests proving it respects the same auth/session semantics as the primary transport.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remote-transport-session-isolation/02-CONTEXT.md
@.planning/phases/02-remote-transport-session-isolation/02-RESEARCH.md
@.planning/phases/02-remote-transport-session-isolation/02-PATTERNS.md
@.planning/phases/02-remote-transport-session-isolation/02-01-SUMMARY.md
@.planning/phases/02-remote-transport-session-isolation/02-02-SUMMARY.md

@server.php
@classes/local/transport/server.php
@classes/local/auth/transport_identity.php
@classes/local/stream/session_store.php
@classes/local/stream/replay_store.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dedicated SSE compatibility controller and endpoint</name>
  <files>sse.php, classes/local/transport/sse_controller.php</files>
  <read_first>server.php, classes/local/transport/server.php, classes/local/auth/transport_identity.php, classes/local/stream/session_store.php, classes/local/stream/replay_store.php, .planning/phases/02-remote-transport-session-isolation/02-CONTEXT.md</read_first>
  <action>Create `classes/local/transport/sse_controller.php` and a dedicated `sse.php` entrypoint for legacy compatibility mode per D-03 and D-12. The SSE path must validate Origin and plugin-managed session state, use the same plugin-side auth semantics as the primary transport, and emit SSE-compatible headers without changing the source of truth for auth, discovery, or execution. Do not overload `server.php` with legacy SSE-only behavior; keep the compatibility adapter separate.</action>
  <verify>rtk php -l sse.php && rtk php -l classes/local/transport/sse_controller.php && rtk rg "text/event-stream|MCP-Session-Id|Origin" sse.php classes/local/transport/sse_controller.php</verify>
  <acceptance_criteria>
    - `sse.php` exists as a separate compatibility entrypoint.
    - `sse_controller.php` exists and uses plugin-owned session/auth state instead of a separate authority.
    - SSE response handling emits `text/event-stream` behavior without turning SSE into the primary transport path.
  </acceptance_criteria>
  <done>Legacy SSE compatibility exists as a bounded adapter over the primary transport/session foundation.</done>
</task>

<task type="auto">
  <name>Task 2: Add automated tests for SSE compatibility behavior</name>
  <files>tests/sse_transport_test.php</files>
  <read_first>tests/server_test.php, classes/local/transport/sse_controller.php, sse.php</read_first>
  <action>Create `tests/sse_transport_test.php` to cover at minimum: SSE compatibility endpoint existence, session-id validation behavior, and the fact that compatibility mode does not bypass plugin-side transport identity semantics. Keep tests targeted at compatibility behavior rather than asserting SSE is the preferred transport.</action>
  <verify>rtk php -l tests/sse_transport_test.php && rtk rg "test_.*sse|test_.*session|test_.*compat" tests/sse_transport_test.php</verify>
  <acceptance_criteria>
    - `tests/sse_transport_test.php` exists and follows repo PHPUnit conventions.
    - Test names cover SSE compatibility and session validation behavior.
    - Tests express that SSE is compatibility mode, not the main transport authority.
  </acceptance_criteria>
  <done>SSE compatibility behavior is pinned by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk php -l sse.php classes/local/transport/sse_controller.php tests/sse_transport_test.php`
- [ ] `rtk rg "text/event-stream|MCP-Session-Id|Origin" sse.php classes/local/transport/sse_controller.php`
- [ ] `rtk rg "test_.*sse|test_.*session|test_.*compat" tests/sse_transport_test.php`
</verification>

<success_criteria>

- All tasks completed
- Legacy SSE exists only as a compatibility adapter
- SSE respects plugin-owned session/auth state
- SSE compatibility behavior is covered by tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-remote-transport-session-isolation/02-03-SUMMARY.md`
</output>
