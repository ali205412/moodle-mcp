---
phase: 02-remote-transport-session-isolation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - db/caches.php
  - settings.php
  - lang/en/webservice_mcp.php
  - classes/local/transport/origin_validator.php
  - classes/local/stream/session_store.php
  - classes/local/stream/replay_store.php
  - tests/transport_state_test.php
autonomous: true
requirements:
  - TRAN-03
  - TRAN-04
user_setup: []
must_haves:
  truths:
    - "Transport endpoints have explicit Origin allowlisting and browser-safe preflight policy instead of wildcard CORS."
    - "Per-client MCP session state is stored in plugin-managed transport/session storage rather than ad hoc in-process globals."
    - "Transport state helpers and policies are covered by automated tests."
  artifacts:
    - path: "db/caches.php"
      provides: "Cache definitions for MCP session and replay state"
      min_lines: 25
    - path: "classes/local/transport/origin_validator.php"
      provides: "Origin allowlist validation helper"
      min_lines: 60
    - path: "classes/local/stream/session_store.php"
      provides: "MCP session metadata store"
      min_lines: 80
    - path: "classes/local/stream/replay_store.php"
      provides: "Replay/event buffer abstraction"
      min_lines: 60
    - path: "tests/transport_state_test.php"
      provides: "Automated coverage for transport state and CORS policy helpers"
      min_lines: 70
  key_links:
    - from: "settings.php"
      to: "classes/local/transport/origin_validator.php"
      via: "Origin validator reads plugin transport settings and enforces allowlist behavior"
    - from: "db/caches.php"
      to: "classes/local/stream/session_store.php"
      via: "session store uses plugin-defined cache area for MCP session state"
    - from: "db/caches.php"
      to: "classes/local/stream/replay_store.php"
      via: "replay store uses plugin-defined cache area for replay/event data"
---

<objective>
Create the transport-state and policy foundation for browser-safe remote MCP transport.

Purpose: Deliver the non-negotiable state and policy pieces behind TRAN-03 and TRAN-04 before the main transport server is refactored.
Output: Cache definitions, explicit Origin allowlist handling, plugin-managed session/replay stores, and automated tests for those helpers.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remote-transport-session-isolation/02-CONTEXT.md
@.planning/phases/02-remote-transport-session-isolation/02-RESEARCH.md
@.planning/phases/02-remote-transport-session-isolation/02-PATTERNS.md

@settings.php
@lang/en/webservice_mcp.php
@tmp/moodle/lib/classes/session/manager.php
@tmp/moodle/lib/classes/router/middleware/cors_middleware.php
@tmp/moodle/lib/classes/router/middleware/moodle_bootstrap_middleware.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add transport settings and cache definitions</name>
  <files>db/caches.php, settings.php, lang/en/webservice_mcp.php</files>
  <read_first>settings.php, lang/en/webservice_mcp.php, .planning/phases/02-remote-transport-session-isolation/02-CONTEXT.md, .planning/phases/02-remote-transport-session-isolation/02-RESEARCH.md</read_first>
  <action>Create `db/caches.php` with concrete cache definitions for MCP session metadata and replay/event storage per D-09. Extend `settings.php` with Phase 2 transport settings for an explicit Origin allowlist, enabling legacy SSE compatibility, and any transport state TTL needed for session/replay storage. Add the matching strings to `lang/en/webservice_mcp.php`. Do not leave wildcard CORS as an implicit default; the settings layer must be able to express an allowlist-driven policy per D-07.</action>
  <verify>rtk php -l db/caches.php && rtk php -l settings.php && rtk php -l lang/en/webservice_mcp.php && rtk rg "stream|replay|origin|sse" db/caches.php settings.php lang/en/webservice_mcp.php</verify>
  <acceptance_criteria>
    - `db/caches.php` defines separate plugin cache areas for MCP session state and replay/event data.
    - `settings.php` contains transport settings for Origin allowlisting and SSE compatibility.
    - `lang/en/webservice_mcp.php` contains matching transport-setting strings.
  </acceptance_criteria>
  <done>Phase 2 transport policy and state settings are explicit and plugin-configurable.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Origin validator and session/replay stores</name>
  <files>classes/local/transport/origin_validator.php, classes/local/stream/session_store.php, classes/local/stream/replay_store.php</files>
  <read_first>classes/local/auth/credential_manager.php, classes/local/auth/transport_identity.php, tmp/moodle/lib/classes/session/manager.php, tmp/moodle/lib/classes/router/middleware/cors_middleware.php, .planning/phases/02-remote-transport-session-isolation/02-RESEARCH.md</read_first>
  <action>Create `classes/local/transport/origin_validator.php` to replace wildcard CORS with explicit allowlist evaluation, safe handling of no-Origin requests, and preflight-safe origin validation per D-07 and D-08. Create `classes/local/stream/session_store.php` for generating, persisting, touching, loading, and deleting `MCP-Session-Id` metadata with plugin-managed state per D-09. Create `classes/local/stream/replay_store.php` as a minimal replay abstraction that can append and fetch replay items without making replay the transport authority. Keep all state plugin-owned and do not push auth/discovery semantics into the companion seam.</action>
  <verify>rtk php -l classes/local/transport/origin_validator.php && rtk php -l classes/local/stream/session_store.php && rtk php -l classes/local/stream/replay_store.php && rtk rg "MCP-Session-Id|origin|allowlist|replay" classes/local/transport/origin_validator.php classes/local/stream/session_store.php classes/local/stream/replay_store.php</verify>
  <acceptance_criteria>
    - `origin_validator.php` exists and exposes explicit allow/deny behavior instead of wildcard-only headers.
    - `session_store.php` exists and includes create/get/touch/delete behavior for per-client transport sessions.
    - `replay_store.php` exists and provides append/fetch/prune-style replay abstraction.
  </acceptance_criteria>
  <done>Transport policy and session-state services exist behind plugin-owned abstractions.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for transport policy and state helpers</name>
  <files>tests/transport_state_test.php</files>
  <read_first>tests/server_test.php, classes/local/transport/origin_validator.php, classes/local/stream/session_store.php, classes/local/stream/replay_store.php</read_first>
  <action>Create `tests/transport_state_test.php` using existing PHPUnit conventions. Cover at minimum: allowed vs denied Origin evaluation, safe no-Origin handling, session id creation and lookup, session invalidation/deletion, and minimal replay append/fetch behavior. The tests should prove the plugin owns transport state per D-09 and browser policy per D-07/D-08.</action>
  <verify>rtk php -l tests/transport_state_test.php && rtk rg "test_.*origin|test_.*session|test_.*replay" tests/transport_state_test.php</verify>
  <acceptance_criteria>
    - `tests/transport_state_test.php` exists and follows repo PHPUnit conventions.
    - Test names cover Origin allow/deny behavior and session store lifecycle.
    - Test names cover replay store append/fetch or equivalent minimal behavior.
  </acceptance_criteria>
  <done>Transport policy/state helpers are protected by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk php -l db/caches.php settings.php classes/local/transport/origin_validator.php classes/local/stream/session_store.php classes/local/stream/replay_store.php tests/transport_state_test.php`
- [ ] `rtk rg "stream|replay|origin|sse" db/caches.php settings.php lang/en/webservice_mcp.php`
- [ ] `rtk rg "MCP-Session-Id|allowlist|replay" classes/local/transport/origin_validator.php classes/local/stream/session_store.php classes/local/stream/replay_store.php`
- [ ] `rtk rg "test_.*origin|test_.*session|test_.*replay" tests/transport_state_test.php`
</verification>

<success_criteria>

- All tasks completed
- Transport policy and state are plugin-managed
- Origin allowlisting replaces wildcard-only CORS
- Session/replay stores exist and are covered by tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-remote-transport-session-isolation/02-01-SUMMARY.md`
</output>
