---
phase: 02-remote-transport-session-isolation
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - server.php
  - classes/local/server.php
  - classes/local/transport/server.php
  - classes/local/transport/protocol_headers.php
  - tests/transport_server_test.php
autonomous: true
requirements:
  - TRAN-01
  - TRAN-03
  - TRAN-04
  - TRAN-05
user_setup: []
must_haves:
  truths:
    - "Authenticated clients can initialize and invoke tools through a Streamable HTTP-oriented HTTPS endpoint with correct transport headers."
    - "Preflight succeeds before auth, and authenticated transport requests enforce protocol/session header expectations."
    - "Long-lived or wait-heavy transport handling releases the Moodle session lock instead of blocking normal browsing."
  artifacts:
    - path: "classes/local/transport/server.php"
      provides: "Primary transport orchestration for Streamable HTTP"
      min_lines: 120
    - path: "classes/local/transport/protocol_headers.php"
      provides: "Header parsing and validation for MCP transport requests"
      min_lines: 70
    - path: "server.php"
      provides: "Entrypoint wired to the new transport server"
      min_lines: 20
    - path: "tests/transport_server_test.php"
      provides: "Transport behavior tests"
      min_lines: 80
  key_links:
    - from: "server.php"
      to: "classes/local/transport/server.php"
      via: "entrypoint instantiates the primary transport server"
    - from: "classes/local/transport/server.php"
      to: "classes/local/transport/protocol_headers.php"
      via: "transport server validates protocol and session headers before request execution"
    - from: "classes/local/transport/server.php"
      to: "classes/local/stream/session_store.php"
      via: "transport server issues and validates MCP session ids"
---

<objective>
Refactor the main MCP endpoint into a Streamable HTTP-aware transport server with safe preflight/auth/session ordering.

Purpose: Deliver TRAN-01, TRAN-03, TRAN-04, and TRAN-05 by moving the main transport semantics into dedicated transport classes while preserving compatibility with the existing plugin core.
Output: New transport server and protocol-header helper, updated entrypoint wiring, and automated transport behavior tests.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-remote-transport-session-isolation/02-CONTEXT.md
@.planning/phases/02-remote-transport-session-isolation/02-RESEARCH.md
@.planning/phases/02-remote-transport-session-isolation/02-PATTERNS.md
@.planning/phases/02-remote-transport-session-isolation/02-01-SUMMARY.md

@server.php
@classes/local/server.php
@classes/local/auth/transport_identity.php
@tmp/moodle/webservice/lib.php
@tmp/moodle/lib/classes/session/manager.php
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protocol header parsing and validation helpers</name>
  <files>classes/local/transport/protocol_headers.php</files>
  <read_first>classes/local/request.php, tmp/moodle/webservice/lib.php, .planning/phases/02-remote-transport-session-isolation/02-RESEARCH.md, .planning/phases/02-remote-transport-session-isolation/02-CONTEXT.md</read_first>
  <action>Create `classes/local/transport/protocol_headers.php` to parse and validate `MCP-Protocol-Version`, `MCP-Session-Id`, `Mcp-Method`, and `Mcp-Name` on transport requests. Accept the negotiated/default protocol version needed by this project, reject invalid or unsupported values with concrete HTTP 400 behavior, and enforce that non-initialization stateful requests provide `MCP-Session-Id` per the current transport contract. Keep the helper independent from transport auth and execution logic so later phases can reuse it.</action>
  <verify>rtk php -l classes/local/transport/protocol_headers.php && rtk rg "MCP-Protocol-Version|MCP-Session-Id|Mcp-Method|Mcp-Name" classes/local/transport/protocol_headers.php</verify>
  <acceptance_criteria>
    - `protocol_headers.php` exists and explicitly handles `MCP-Protocol-Version`, `MCP-Session-Id`, `Mcp-Method`, and `Mcp-Name`.
    - The helper distinguishes initialization from subsequent stateful requests.
    - The helper encodes explicit bad-request behavior for invalid or missing required headers.
  </acceptance_criteria>
  <done>Transport header semantics are expressed in one reusable helper instead of being implicit in ad hoc request handling.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Streamable HTTP transport server and entrypoint handoff</name>
  <files>classes/local/transport/server.php, server.php, classes/local/server.php</files>
  <read_first>classes/local/server.php, server.php, classes/local/auth/transport_identity.php, classes/local/stream/session_store.php, classes/local/transport/origin_validator.php, tmp/moodle/webservice/lib.php, tmp/moodle/lib/classes/session/manager.php</read_first>
  <action>Create `classes/local/transport/server.php` as the primary transport orchestrator per D-10, using the existing `classes/local/server.php` as a compatibility shell where useful instead of adding more monolith logic there. Wire `server.php` to instantiate the new transport server. In the new transport flow: handle OPTIONS before auth per D-08, validate Origin per D-07, validate transport headers via `protocol_headers.php`, resolve plugin-side transport identity, create/validate `MCP-Session-Id` state, and call `\core\session\manager::write_close()` after auth before long-lived or wait-heavy transport handling per D-05. Keep the plugin authoritative for auth/execution; do not move those decisions into the companion seam.</action>
  <verify>rtk php -l classes/local/transport/server.php && rtk php -l server.php && rtk rg "write_close\\(|OPTIONS|MCP-Session-Id|Origin" classes/local/transport/server.php server.php</verify>
  <acceptance_criteria>
    - `server.php` instantiates the new transport server.
    - `classes/local/transport/server.php` handles preflight before auth and uses explicit Origin validation.
    - `classes/local/transport/server.php` issues or validates `MCP-Session-Id` and calls `\core\session\manager::write_close()` after auth in the long-lived transport path.
  </acceptance_criteria>
  <done>Main transport entrypoint is Streamable HTTP-aware and session-safe.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated tests for transport request lifecycle</name>
  <files>tests/transport_server_test.php</files>
  <read_first>tests/server_test.php, classes/local/transport/server.php, classes/local/transport/protocol_headers.php, classes/local/transport/origin_validator.php</read_first>
  <action>Create `tests/transport_server_test.php` following the existing PHPUnit style. Cover at minimum: OPTIONS/preflight handled before auth, header validation for `MCP-Protocol-Version` and `MCP-Session-Id`, initialize behavior that returns a session id when the server is stateful, and the long-lived transport path releasing session lock through the transport flow. Keep assertions concrete and source-backed rather than generic transport placeholders.</action>
  <verify>rtk php -l tests/transport_server_test.php && rtk rg "test_.*options|test_.*protocol|test_.*session|test_.*write_close" tests/transport_server_test.php</verify>
  <acceptance_criteria>
    - `tests/transport_server_test.php` exists and follows repo PHPUnit conventions.
    - The test names cover preflight/auth ordering, protocol header validation, and session-id behavior.
    - The test file includes explicit coverage for the post-auth session-release behavior.
  </acceptance_criteria>
  <done>Primary transport request lifecycle behavior is pinned by automated tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `rtk php -l classes/local/transport/protocol_headers.php classes/local/transport/server.php tests/transport_server_test.php server.php`
- [ ] `rtk rg "MCP-Protocol-Version|MCP-Session-Id|Mcp-Method|Mcp-Name" classes/local/transport/protocol_headers.php`
- [ ] `rtk rg "write_close\\(|OPTIONS|Origin|MCP-Session-Id" classes/local/transport/server.php server.php`
- [ ] `rtk rg "test_.*options|test_.*protocol|test_.*session|test_.*write_close" tests/transport_server_test.php`
</verification>

<success_criteria>

- All tasks completed
- Primary HTTP transport is Streamable HTTP-aware
- Preflight/auth/session ordering is explicit and safe
- Long-lived transport no longer holds the Moodle session lock unnecessarily
</success_criteria>

<output>
After completion, create `.planning/phases/02-remote-transport-session-isolation/02-02-SUMMARY.md`
</output>
